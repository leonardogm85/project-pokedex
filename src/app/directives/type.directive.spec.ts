import { TypeDirective } from './type.directive';

describe('TypeDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeDirective();
    expect(directive).toBeTruthy();
  });
});
